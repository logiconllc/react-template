import React from 'react';

const SampleComponent = ({repoName}) => {
    return (
        <div>
            This is a sample components with a sample repoName as Prop: {repoName}
        </div>
    );
};

export default SampleComponent;